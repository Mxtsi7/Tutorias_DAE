-- Modal.lua: Componente de ventana modal para confirmaciones y detalles

local Modal = {}
Modal.__index = Modal

function Modal.new(titulo, contenido, opciones)
    -- TODO: crear modal con título, contenido y botones de acción
end

function Modal:show()
    -- TODO: activar visibilidad con animación de entrada
end

function Modal:hide()
    -- TODO: ocultar con animación de salida
end

function Modal:draw()
    -- TODO: dibujar fondo oscuro, ventana centrada, título y botones
end

return Modal
