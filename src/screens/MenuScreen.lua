-- MenuScreen.lua: Pantalla principal del sistema de tutorías

local MenuScreen = {}

function MenuScreen:load()
    -- TODO: inicializar botones principales:
    -- "Nueva Solicitud", "Ver Tutorías Activas", "Registrar Sesión", "Seguimiento"
end

function MenuScreen:update(dt)
    -- TODO: actualizar animaciones o timers del menú
end

function MenuScreen:draw()
    -- TODO: dibujar título, botones y estado general del sistema
end

function MenuScreen:mousepressed(x, y, button)
    -- TODO: detectar clic en botones y navegar a pantalla correspondiente
end

return MenuScreen
